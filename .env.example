# PostgreSQL Configuration
POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpass
POSTGRES_DB=main_db
POSTGRES_PORT=5432
POSTGRES_HOST=postgres

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379

# Django Secret Keys (cambiar en producción)
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
AUTH_SECRET_KEY=django-insecure-change-this-in-production
BLOG_SECRET_KEY=django-insecure-blog-service-key-change-in-production
EMAIL_SECRET_KEY=django-insecure-email-service-key-change-in-production

# Email Configuration (opcional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password

# Debug Mode (0 = False, 1 = True)
DEBUG=1

# CORS Settings (separar por comas)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
